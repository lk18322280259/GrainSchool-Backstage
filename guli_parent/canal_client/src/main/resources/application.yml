#### application.yml
spring:
  application:
    name: canal-client
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 需要同步到的库
    url: jdbc:mysql://43.138.111.63:3316/guli?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: MySQL5.7
  profiles:
    active: dev
####  application-dev.yml
server:
  port: 10001
